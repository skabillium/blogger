{
  "name": "blog",
  "version": "1.0.0",
  "description": "A blog website created to get familiar with the mern stack",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \" npm run server \" \" npm run client \"  ",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "install-server": "npm install --prefix server"
  },
  "author": "skabi",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.1.0"
  }
}
